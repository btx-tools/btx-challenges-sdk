## Summary

<!-- 1-3 bullet points describing what changed and why. -->

## Testing

- [ ] `pnpm -r test:unit` passes locally
- [ ] `pnpm -r build && pnpm -r type-check` clean
- [ ] `pnpm -r exec prettier --check "src/**/*.ts" "tests/**/*.ts"` clean
- [ ] CHANGELOG updated (if user-facing)

## Notes

<!-- Anything reviewers should know — design tradeoffs, follow-up work, related issues. -->

<!-- For security-sensitive changes: see SECURITY.md. -->
