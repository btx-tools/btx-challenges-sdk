{
  "name": "@btx-tools/example-browser-solver",
  "version": "0.0.0",
  "private": true,
  "description": "Vite browser page that hits a BTX-gated endpoint, solves the challenge in a Web Worker, and renders per-attempt timings",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@btx-tools/challenges-sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  },
  "engines": {
    "node": ">=18.17"
  }
}
