{
  "name": "@btx-tools/example-express-gate",
  "version": "0.0.0",
  "private": true,
  "description": "Express app with a BTX-gated POST /v1/generate route + a Node client that walks the 402→solve→200 flow",
  "type": "module",
  "scripts": {
    "start:server": "tsx src/server.ts",
    "start:client": "tsx src/client.ts",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@btx-tools/challenges-sdk": "workspace:*",
    "@btx-tools/middleware-express": "workspace:*",
    "cors": "^2.8.5",
    "express": "^5.0.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18.17"
  }
}
